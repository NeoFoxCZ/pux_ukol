- Přidat spíše FileSystemWatcher a vše mít uložené v cache. 
Aby se nemusela číst celá složka opakovaně.
- Pouzil jsem Primary constructor
- Pro toto MVC bych idealne použil minimal API
- Pro jednoduchost a při přidávání dalších endpointů bych použil i swagger (zde nebyl nutný)
- 3️⃣ Vyhněte se uložení velkých objektů v cache (Dictionary<string, (int? version, DateTime lastModified)>)
  Pokud váš oldFileData obsahuje velké množství záznamů, zkuste místo toho:
  
  Použít LazyCache nebo jiný slabý cache mechanismus.
  Nastavit expirační dobu pro cache.Set("FileList", newFileData, TimeSpan.FromMinutes(10));
  